<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="applicationName" source="spring.application.name" defaultValue="kill-e"/>
    <springProperty scope="context" name="environment" source="spring.profiles.active" defaultValue="dev"/>

    <!-- JSON Console Appender for Production -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"${applicationName}","environment":"${environment}"}</customFields>
            <includeMdcKeyName>correlationId</includeMdcKeyName>
            <includeMdcKeyName>layer</includeMdcKeyName>
            <includeMdcKeyName>className</includeMdcKeyName>
            <includeMdcKeyName>methodName</includeMdcKeyName>
            <includeMdcKeyName>executionTimeMs</includeMdcKeyName>
            <includeMdcKeyName>useCaseType</includeMdcKeyName>
            <includeMdcKeyName>operationType</includeMdcKeyName>
            <includeMdcKeyName>repositoryClass</includeMdcKeyName>
            <includeMdcKeyName>httpMethod</includeMdcKeyName>
            <includeMdcKeyName>uri</includeMdcKeyName>
            <includeMdcKeyName>statusCode</includeMdcKeyName>
            <includeMdcKeyName>exceptionType</includeMdcKeyName>
            <includeMdcKeyName>exceptionMessage</includeMdcKeyName>
            <includeMdcKeyName>slowQuery</includeMdcKeyName>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Human-Readable Console Appender for Development -->
    <appender name="HUMAN_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} [%X{correlationId}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/kill-e.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"${applicationName}","environment":"${environment}"}</customFields>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/kill-e.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- Async Appender for Performance -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- Development Profile - Human Readable -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="HUMAN_CONSOLE"/>
        </root>
        <logger name="com.yourapp" level="DEBUG" additivity="false">
            <appender-ref ref="HUMAN_CONSOLE"/>
        </logger>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

    <!-- Production Profile - JSON -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
        <logger name="com.yourapp" level="INFO" additivity="false">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </logger>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>

    <!-- Default (no profile) - Human Readable -->
    <springProfile name="!prod &amp; !dev">
        <root level="INFO">
            <appender-ref ref="HUMAN_CONSOLE"/>
        </root>
        <logger name="com.yourapp" level="DEBUG" additivity="false">
            <appender-ref ref="HUMAN_CONSOLE"/>
        </logger>
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
    </springProfile>
</configuration>
